import 'package:flutter/material.dart';

class pdf_listScreen extends StatelessWidget {
   
  const pdf_listScreen({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('pdf_listScreen'),
      ),
    );
  }
}